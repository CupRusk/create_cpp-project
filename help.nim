import mini_lib

proc help*() = 
    write("1. Пример команды обычного старта: create_project --default main\n")
    write("2. Пример команды функционального старта: create_project --function main\n")
    write("3. Пример команды SOLID(ООП) старта: create_project --classes main\n")
